<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:staticBirthdays="clr-namespace:Personnel.Application.ViewModels.Birthdays;assembly=Personnel.Application.ViewModels"
                    xmlns:localConverters="clr-namespace:Personnel.Appn.Converters"
                    xmlns:helperConverterss="clr-namespace:Helpers.WPF.Converters;assembly=Helpers.WPF"
                    xmlns:controls="clr-namespace:Personnel.Appn.Controls"
                    xmlns:static="clr-namespace:Personnel.Application.ViewModels;assembly=Personnel.Application.ViewModels"
                    xmlns:md="clr-namespace:MaterialDesignThemes.Wpf;assembly=MaterialDesignThemes.Wpf">

    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Button.xaml" />
        <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.CheckBox.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <DataTemplate x:Key="BirthdaysTimePartTemplate" DataType="staticBirthdays:TimePartViewModel">
        <md:Card>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                

                <md:ColorZone MinHeight="30" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
                    <md:ColorZone.Style>
                        <Style TargetType="md:ColorZone">
                            <Setter Property="Mode" Value="Light"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCurrent}" Value="True">
                                    <Setter Property="Mode" Value="Accent"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </md:ColorZone.Style>
                </md:ColorZone>
                <TextBlock Text="{Binding Name}" TextWrapping="Wrap" TextTrimming="WordEllipsis"/>
            </Grid>
        </md:Card>
    </DataTemplate>

    <DataTemplate x:Key="BirthdaysLevelTemplate" DataType="staticBirthdays:LevelViewModel">
        <Grid>
            <ScrollViewer Margin="16" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Parts}"
                              ItemTemplate="{DynamicResource BirthdaysTimePartTemplate}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal" ItemWidth="320"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </DataTemplate>

</ResourceDictionary>